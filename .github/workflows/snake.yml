name: Generate Snake Animation

on:
  push:
    branches:
      - main  # Se ejecutará cuando realices un push a la rama main
  schedule:
    - cron: "0 0 * * *"  # Esto se ejecutará todos los días a medianoche
  workflow_dispatch:  # Permite ejecutar manualmente desde GitHub

jobs:
  generate:
    runs-on: ubuntu-latest  # Ejecutará en una máquina con Ubuntu

    steps:
      - name: Generar animación de la serpiente
        uses: Platane/snk@v3  # Usa la acción Platane para generar la serpiente
        with:
          github_user_name: PabloCodevs  # Tu nombre de usuario de GitHub
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      - name: Hacer commit y push
        uses: EndBug/add-and-commit@v9  # Esta acción hace commit y push
        with:
          message: "🐍 Actualiza la animación de la serpiente"
          add: "dist/*.svg"  # Asegúrate de que las imágenes generadas se añaden al commit
